#include <stdio.h>
#include <stdlib.h>
int main(int argc, char **argv)
{
	int entero;

	if (argc != 2)
	{
		printf("Error\n");
			exit(1);
	}
	entero = atoi(argv[1]);
	if (entero < 0)
	{
		printf("Error\n");
		exit(2);
	}
}
